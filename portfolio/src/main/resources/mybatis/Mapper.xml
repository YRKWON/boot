<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yr.dao.portfolioDao">
	<insert id="Insert"
		parameterType="com.yr.vo.Contact">
		insert into contact values (#{name},#{email},#{subject},#{message},sysdate())
	</insert>
</mapper>